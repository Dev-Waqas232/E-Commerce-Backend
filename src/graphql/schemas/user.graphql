type Query {
  users: [User!]!
}

type Mutation {
  createUser(name: String!): [User!]!
}

type User {
  id: ID!
  firstName: String!
  lastName: String!
  email: String!
  role: Role!
}

enum Role {
  admin
  instructor
  student
}
